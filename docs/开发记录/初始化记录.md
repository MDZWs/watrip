# 项目初始化记录

> 初始化时间：2026-07-02
> 工作流：SpecForge 项目级 - Skill 7
> 项目：旅行 App 社区化升级

---

## 1. 环境检查

| 检查项 | 状态 |
|--------|------|
| specs/产品概述.md | 已就绪 |
| specs/技术栈.md | 已就绪 |
| specs/项目结构.md | 已就绪 |
| specs/开发规范.md | 已就绪 |
| specs/开发路线图.md | 已就绪 |
| 现有代码（css/ js/ index.html） | 保留，未改动逻辑 |
| 高德 API Key | 已配置 |
| Supabase 配置 | 已添加配置项（值待填） |

---

## 2. 执行内容

### 2.1 配置文件
- [x] 修改 `js/config.js`：增量添加 `SUPABASE_URL`、`SUPABASE_ANON_KEY` 配置项（值留空待填）
- [x] 创建 `.env.example`：环境变量模板（Supabase / 高德 / 阿里云内容安全）

### 2.2 基础层（lib/ + shared/ + css/）
- [x] 创建 `lib/supabase-client.js`：Supabase 客户端初始化与单例，导出全局 `supabase` 实例
- [x] 创建 `shared/ui-kit.js`：公共 UI 工具（toast/confirm/loading/emptyState）+ EventBus 事件总线
- [x] 创建 `css/ui-kit.css`：公共 UI 组件样式

### 2.3 目录结构
已创建以下目录（通过文件创建自动生成）：
```
lib/                # 第三方集成封装
shared/             # 公共工具
docs/开发记录/      # 开发记录文档
```

待开发时按需创建：
```
modules/community/          # Phase 2 启用
modules/creator-system/     # Phase 3 启用
modules/checkin/            # Phase 4 启用
modules/pdf-export/         # Phase 3.2 启用
```

### 2.4 入口文件
- [x] 修改 `index.html`：
  - 引入 `css/ui-kit.css`
  - 引入 Supabase JS SDK（CDN）
  - 引入 `lib/supabase-client.js`、`shared/ui-kit.js`
  - 添加功能模块引入占位注释（待开发启用）

---

## 3. 未执行项（按路线图在对应 Phase 启用）

| 项目 | 启用时机 |
|------|---------|
| Supabase 项目注册 + 填入 URL/Key | Phase 1.1 |
| 数据库表创建 + RLS 策略 | Phase 1.1 |
| shared/auth.js（认证） | Phase 1.1 |
| shared/points-core.js（积分核心） | Phase 1.2 |
| shared/storage-migrate.js（数据迁移） | Phase 1.3 |
| lib/amap-helper.js（高德封装） | Phase 1.2 |
| modules/community/*（专栏） | Phase 2 |
| modules/creator-system/*（价值体系） | Phase 3 |
| modules/checkin/*（打卡吐槽） | Phase 4 |
| modules/pdf-export/*（PDF 导出） | Phase 3.2 |

---

## 4. 使用前准备

1. 注册 Supabase 项目：https://supabase.com
2. 在 `js/config.js` 填入 `SUPABASE_URL` 和 `SUPABASE_ANON_KEY`
3. 按 `specs/开发路线图.md` Phase 1.1 开始开发

---

## 5. 注意事项

- 现有 `js/`、`css/` 下文件逻辑未改动，仅 `js/config.js` 增量添加配置项
- `index.html` 仅增量引入新脚本与样式，现有引入顺序不变
- Supabase 配置为空时，`supabase-client.js` 会输出警告并降级（社区功能不可用，但不影响现有行程规划功能）
- EventBus 与 UiKit 已就绪，后续模块可直接使用 `window.EventBus` 和 `window.UiKit`
